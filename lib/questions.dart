class Questions {
  String questionText;
  String questionImage;
  bool questionAnswer;
  Questions({
    required String q,required String i,required bool a
  }) : questionText = q, questionAnswer = a,questionImage=i;


}
